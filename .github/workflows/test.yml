name: Node.js CI

on: push

jobs:
  build:

    runs-on: ubuntu-latest
   
    steps:
    - name: add secret
      id: ami
      run: |
       echo TEST=${{ secrets.TEST }} >> .env.local
       cat .env.local
