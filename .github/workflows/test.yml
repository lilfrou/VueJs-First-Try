name: Node.js CI

on: push

jobs:
  build:

    runs-on: ubuntu-latest
   
    steps:
    - name: check
      id: ami
      run: |
        t1="env:
            AWS_ACCESS_KEY_ID: ***
            AWS_SECRET_ACCESS_KEY: ***
            OVICE_BOT_SECRET_SSH_KEY: ***
            AWS_REGION: ap-northeast-1
            AWS_CLI_VERSION: 1.21.7
            VERBOSE: false
           {"ImageId": "ami-006b180665fe4e6ec"}"
        echo "::set-output name=deployment_env::$(echo $t1 | grep ami)"
    - name: install
      run: | 
       echo ${{ steps.ami.outputs.deployment_env }}
