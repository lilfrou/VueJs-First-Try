name: Node.js CI

on: push

jobs:
  build:

    runs-on: ubuntu-latest
   
    steps:
    - name: check
      id: ami
      run: |
        t1="{"ImageId": "ami-006b180665fe4e6ec"}"
        echo "::set-output name=deployment_env::$t1"
    - name: install
      run: | 
       echo ${{ steps.ami.outputs.deployment_env }}
