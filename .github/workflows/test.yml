name: Node.js CI

on: push

jobs:
  build:

    runs-on: ubuntu-latest
   
    steps:
    - name: check
      id: ami
      run: |
        t1=$(cat << EOF
           ami-006b180665fe4e6ec
          EOF)
        echo "::set-output name=deployment_env::$(echo $t1 | grep ami)"
    - name: install
      run: | 
       echo ${{ steps.ami.outputs.deployment_env }}
