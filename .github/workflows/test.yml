name: Node.js CI

on: push

jobs:
  build:

    runs-on: ubuntu-latest
   
    steps:
    - name: add secret
      id: ami
      run: |
       echo ${{ secrets.TEST }} > ami_name.txt
       cat ami_name.txt
